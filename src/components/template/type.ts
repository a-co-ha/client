export interface TemplatePageProps {
  channelId: string;
  pageId: string;
  type: string;
}

export interface PageInTemplateProps {
  channelId: string;
  pageId: string;
  pageName: string;
  type: string;
}
