import * as styles from './styles';

export const Chart = () => {
  return (
    <div css={styles.contentBox}>
      <h3 css={styles.contentBoxTitle}>차트</h3>
      <div css={styles.content}>내용</div>
    </div>
  );
};
