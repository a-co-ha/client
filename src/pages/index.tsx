import { Loading } from '@/components/loading/Loading';
// import { Error } from './error';

const IndexPage = () => {
  return (
    <div>
      <Loading />
    </div>
  );
};

export default IndexPage;
